from typing import List
from app.models.toggl import TogglTimeEntry


def generate_summary(time_entries: List[TogglTimeEntry]) -> str:
    """
    Generate a text summary from time entries.

    Args:
        time_entries: List of TogglTimeEntry objects

    Returns:
        String containing the summary text
    """
    # TODO: Implement text summarization
    return ""
