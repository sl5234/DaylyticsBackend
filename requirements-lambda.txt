# Lean dependency set for the daily workflow Lambda (TOGGL_API mode only).
# Excludes: uvicorn (no web server in Lambda), pdfplumber (PDF mode unused
# here), pytest/black/ruff/mypy (dev tools), boto3 (provided by the Lambda
# Python runtime natively - not bundled, to keep the package small).
fastapi==0.115.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.0
appdevcommons>=0.1.2,<1.0.0
openai>=1.0.0
